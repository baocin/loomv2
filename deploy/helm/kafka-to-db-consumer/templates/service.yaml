apiVersion: v1
kind: Service
metadata:
  name: {{ include "kafka-to-db-consumer.fullname" . }}
  labels:
    {{- include "kafka-to-db-consumer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kafka-to-db-consumer.selectorLabels" . | nindent 4 }}
